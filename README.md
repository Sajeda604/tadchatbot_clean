# 🤖 مساعد المكتب البرمجي الذكي

تطبيق ذكي يستخدم تقنيات الذكاء الاصطناعي والمعالجة الطبيعية للغة للإجابة عن الأسئلة المتعلقة بخدمات المكتب البرمجية ومشاريعه.

## ✨ المميزات

- 🏥 **استشارات طبية ذكية**: يجيب على الأسئلة الطبية بناءً على قاعدة معرفية شاملة
- 🌍 **دعم اللغة العربية**: واجهة كاملة باللغة العربية مع دعم RTL
- 🔍 **البحث الموسع**: يبحث عن أفضل المعلومات من قاعدة البيانات
- 💬 **محادثة طبيعية**: يقترح أسئلة متابعة ذكية
- 🎯 **دقة عالية**: يستخدم نماذج LLM متقدمة من Groq

## 🚀 خطوات التشغيل المحلي

### 1️⃣ إنشاء البيئة الافتراضية
```bash
python -m venv venv
```

### 2️⃣ تفعيل البيئة الافتراضية
```bash
# على Windows
.\venv\Scripts\activate

# على macOS/Linux
source venv/bin/activate
```

### 3️⃣ تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 4️⃣ بناء قاعدة FAISS من البيانات
ضع ملفات البيانات (PDF أو TXT) في مجلد `data/` ثم قم بتشغيل (أحد الخيارين):

#### الخيار الموصى به
```bash
python faiss_build.py
```

#### خيار التوافق مع السكربت القديم (اختياري)
لا يزال الملف `create_memory_for_llm.py` موجودًا لأغراض التوافق، ويمكنه استدعاء نفس منطق البناء، لكن يفضل استخدام `faiss_build.py` مباشرة.

### 5️⃣ اختبار صحة ملف FAISS (اختياري لكن مستحسن)
بعد بناء قاعدة FAISS، يمكنك التحقق من أن ملف `index.faiss` صالح وقابل للقراءة عبر:

```bash
python -m faiss_valid_checker
```

### 6️⃣ تشغيل التطبيق
```bash
streamlit run app.py
```

التطبيق سيفتح تلقائياً على: `http://localhost:8501`

## 📁 هيكل المشروع (مبسط)

```
project_sajeda/
├── app.py                      # التطبيق الرئيسي (Streamlit)
├── faiss_build.py              # سكربت بناء قاعدة FAISS من data/
├── faiss_valid_checker.py      # سكربت اختبار index.faiss (python -m faiss_valid_checker)
├── create_memory_for_llm.py    # ملف قديم لأغراض التوافق (يستدعي منطق البناء الجديد)
├── connect_memory_with_llm.py  # سكربت اختباري من سطر الأوامر لسلسلة RAG
├── scripts/
│   └── build_and_push.sh       # سكربت مساعد لبناء FAISS + git-lfs + git push
├── requirements.txt            # المكتبات المطلوبة
├── .env                        # متغيرات البيئة (مفاتيح API) – لا تُرفع للمستودع
├── .gitattributes              # إعداد git-lfs لملفات FAISS
├── data/                       # مجلد البيانات (ملفات PDF/TXT)
└── vectorstore/               # مجلد قاعدة بيانات FAISS
    └── db_faiss/              # ملفات قاعدة البيانات (index.faiss, index.pkl)
```

## 🔑 متغيرات البيئة

يجب إضافة المفاتيح التالية في ملف `.env` (محليًا) أو في إعدادات Railway:

```env
GROQ_API_KEY="your_groq_api_key_here"       # مفتاح Groq للنموذج LLM
HF_TOKEN="your_huggingface_token_here"     # توكن HuggingFace للتضمينات
OPENAI_API_KEY="your_openai_api_key_here"  # (اختياري) فقط إذا أضفت نماذج OpenAI
PYTHONUNBUFFERED=1                          # يفضل ضبطه في Railway لتحسين اللوغات
```

احصل على المفاتيح من:
- **Groq API**: https://console.groq.com
- **HuggingFace Token**: https://huggingface.co/settings/tokens

## 📚 إضافة بيانات جديدة

1. ضع ملفات PDF أو TXT في مجلد `data/`
2. قم بتشغيل: `python faiss_build.py`
3. (اختياري) تحقق من سلامة ملف الفهرس: `python -m faiss_valid_checker`
4. أعد تشغيل التطبيق: `streamlit run app.py`

## 🛠️ المكتبات المستخدمة

- **Streamlit**: واجهة المستخدم
- **LangChain**: إدارة سلاسل المعالجة
- **Groq API**: نموذج اللغة الكبير
- **FAISS**: قاعدة بيانات المتجهات
- **HuggingFace**: نماذج التضمين

## 📦 Git LFS لملفات FAISS

يفضل تتبع ملفات `vectorstore/db_faiss/` عبر git-lfs بدل تخزينها مباشرة في Git، خاصة إذا كان حجمها كبيرًا:

```bash
git lfs install
git lfs track "vectorstore/db_faiss/*"
git add .gitattributes vectorstore/db_faiss/*
git commit -m "chore: track FAISS index via git-lfs"
git push
```

الملف `.gitattributes` يحتوي على السطر التالي:

```text
vectorstore/db_faiss/* filter=lfs diff=lfs merge=lfs -text
```

## 🚀 النشر على Railway

1. قم بإنشاء مشروع جديد على Railway وربطه بمستودع GitHub الذي يحتوي هذا المشروع.
2. تأكد من وجود ملف `Procfile` بالقيمة:

   ```text
   web: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
   ```

3. من إعدادات المشروع في Railway (Environment Variables)، أضف المتغيرات:

   - `GROQ_API_KEY`
   - `HF_TOKEN`
   - `OPENAI_API_KEY` (إن كنت تستخدمه)
   - `PYTHONUNBUFFERED=1`

4. إذا كانت ملفات FAISS كبيرة، تأكد من رفعها إلى GitHub باستخدام git-lfs كما في القسم السابق، أو قم بتشغيل `faiss_build.py` داخل Railway (عبر CI/Job) قبل تشغيل التطبيق.
5. بعد أي تعديل على الكود، ادفع التغييرات إلى GitHub، ثم من لوحة تحكم Railway اختر: **Deploy → Re-deploy** للمشروع.

## 📝 الملاحظات

- تأكد من وجود اتصال إنترنت لتحميل النماذج
- قد يستغرق التشغيل الأول وقتاً لتحميل النماذج
- يمكنك تخصيص Prompt في `app.py` حسب احتياجاتك

## 🤝 المساهمة

يمكنك تحسين المشروع بـ:
- إضافة بيانات طبية أكثر
- تحسين الـ Prompt
- إضافة ميزات جديدة

---

**تم الإنشاء بواسطة**: فريق التطوير
**آخر تحديث**: 2025-11-30
